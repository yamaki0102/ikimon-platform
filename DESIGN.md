# ZUKAN デザインルールブック

Version: **1.2.0**  
Decision date: **2026-09-20 JST**  
Status: **ADOPTED / CANONICAL DESIGN — mainへのmergeをもって発効**  
Canonical: **yamaki0102/ikimon-platform / DESIGN.md**  
Scope: **ブランド表現・UI・コンテンツ・インタラクション・体験品質**

> **地域の記録を、みんなで育てる。**
>
> 世界の多様さが主役になる、静かで信頼できる記録と参加の体験。

一行目は既存の製品メッセージ、二行目は本書が定める内部のデザイン指針である。新しいサービス名・ロゴ・広告タグラインへの変更ではない。

## 0. この正本の使い方

本書は、これからZUKANの画面を設計・実装・評価するための共通判断基準である。既存ページの監査結果、改修提案一覧、コンポーネント実装、稼働証明ではない。**採用されたデザインと、実装・実機検証・本番で成立した体験を分ける。**

対象は公開Web、ログイン後Web、PWA、将来のネイティブアプリ、埋め込み、公開コンテンツ。紙・PDF等はブランドと内容の原則を共有するが、操作・寸法は媒体に合わせる。NOCOSILへ同じ画面・ナビゲーションを強制しない。

**必須**は満たす条件、**原則**は通常の選択、**許容**は理由がある場合の選択を意味する。以下の数値は、外部標準と明記したもの以外はZUKANの設計基準・目標であり、測定済みの成果ではない。

### 正本の責務と優先順位

| 判断すること | 参照する正本 |
|---|---|
| 製品の目的、責務、権利・安全・公開の意味 | 戦略正本、Product Architecture、および対象範囲で後から採用されたGlobal Platform Bundle / Quiet Home契約 [P2][P6][P7] |
| 全画面共通のデザイン原則・表現・品質 | **本書 `DESIGN.md`** |
| ナビゲーション、個別画面、本人・参加の具体的な責務 | `docs/spec/zukan-app-experience/` の既存各契約 |
| 実装・稼働・配備の真実 | current sourceとProduct Registryの静的意味、NOCOSIL Work / Current State / Evidence / Resume、対象runtimeのread-back。Git/旧Boardを実行継続の正本にしない |
| 外部参考資料 | 本書の採用範囲に限る。上記の正本を上書きしない |

権利、安全、アクセシビリティ、法令、本人の選択を美観で弱めない。本書は既存の製品意味論を拡張して権限を与えるものではない。

**2026-09-20の整合訂正:** 9月13日のGlobal Platform Bundle [P6] と9月17日のQuiet Home / Saved [P7] を、その対象範囲で古いHome・親shellの記述より優先する。専門面は同格であり、撮影中心の「通常版」へ従属しない。共通の品質と、各面の入口・主行動を混同しない。既存の動くナビ・URL・撮影・権利を一括変更する指示ではない。

本書の画面レシピは既存契約の実装判断を具体化する。Home/Savedは[P7]、参加は[P10]、自分は[P11]が責務を所有し、本書は第二の個別画面仕様・新しい状態エンジンを作らない。

**限定的な更新:** App Experience §11・§16.2と重なる共通の視覚判断は本書へ集約する。同節の「13–14px」のうち13pxの新規使用は、本書の14px下限により `SUPERSEDED`。既存の色、内容幅、余白、写真優先を継承する。ナビゲーション、ルート、個別画面の構成、§16.5の状態の意味、§17のNOCOSILとの責務分離は維持する。新しいブランド・トークン体系を既存実装へ適用済みとは扱わない。

## 1. ZUKANは何のためのデザインか

ZUKANは、生きものの同定アプリだけでも、地域別に量産する観光サイトでも、投稿量を競うSNSでもない。自然・文化・歴史・仕事・暮らし・活動の記録を、場所、時間、根拠、権利、確認状態と結びつけ、後から知り、参加し、更新できるサービスである。[P1][P2]

**誰かが残した一つの記録が、本人にとっての価値になり、公開できる範囲で地域の知識になる。** この順番を逆転させない。

主な利用者は、初めて見る人、記録する人、参加する人、地域の記録を確かめる人。専門家、学校、自治体、企業、団体も使うが、最初の画面を専門家向けの管理コンソールにしない。熟練度は段階的な情報開示で扱い、国籍や年齢から能力を推測しない。

### Design North Star

> **今ここを知り、残し、次に関われる。**

これは広告コピーではなく、画面設計の内部基準である。主要画面では、利用者がZUKAN固有のtaxonomyや内部用語を知らなくても、**何を見ているか／どこ・いつのことか／今どの状態か・何が未確認か／次に何ができるか**を理解できることを優先する。

機能名やメニューより内容、分類より文脈、最初からやり直す導線より「続きへ戻る」導線を優先する。AI、地図、Program、Place Graph等の仕組みは、理解と行動を助ける場合だけ前面に出す。

「今ここ」はGPS地点だけではなく、本人が選んだ地域・将来の日付・オンラインの到達範囲を含む。**読むだけ、比較だけ、保存だけでも成立した体験である。** 記録や公開を全員の最終行動にしない。食、仕事、旅、催し、特典、学び、文化、自然は同格であり、地域の記録がこれらを支えるが、全画面が投稿アプリになるわけではない。[P6][P7]

### 大切にする五つの体験

| 体験 | 利用者に返す価値 | 成立を確かめる問い |
|---|---|---|
| 見つける | 登録前でも、許可された記録や場所の内容が分かる | 何を、いつ、どの場所について知れたか説明できるか |
| 残す | その場の発見を失わず、自分へ戻せる | どこまで保存されたか分かり、続きから再開できるか |
| 確かめる | AIの候補と、人や資料による確認を見分けられる | 何が未確認で、根拠と訂正先はどこか分かるか |
| 参加する | 自分に合う企画を選び、参加状態を把握できる | いつ・どこで・何に参加し、申込みは成立したか分かるか |
| 見返す・育てる | 場所の時間の重なり、自分や地域の活動の続きを持てる | 過去と現在を比べ、再訪・再記録・訂正へ戻れるか |

初回価値を「登録した」「CTAを押した」だけで測らない。滞在時間、通知開封、投稿件数は補助情報であり、体験の目的そのものにしない。学習効果、地域活性化、生物多様性への効果は、記録・参加の成立と別に検証する。[P1][P2]

## 2. 迷ったときの十二原則

| ID | 原則 | 設計で選ぶこと | 選ばないこと |
|---|---|---|---|
| ZD-01 | 記録と地域を主役にする | 実際の写真・音・資料・時間・場所を見せる | 装飾、巨大なコピー、AIの演出で中身を隠す |
| ZD-02 | まず本人に価値を返す | 保存、再開、見返しを公開と独立させる | 公開しないと役に立たない導線 |
| ZD-03 | 次の一歩を明快にする | 一つの状態で主操作は0–1個、補助は最大2個を基本にする | 同じ強さのCTA群、空のダッシュボード |
| ZD-04 | 状態と根拠に正直である | 保存先、不確かさ、確認対象、公開先を区別する | 成功の先取り、根拠のない「確認済み」 |
| ZD-05 | 多様さを標準とする | 異なる言語、身体条件、端末、通信、地域制度で成立させる | 日本語・高性能スマホ・常時通信だけを通常扱いする |
| ZD-06 | 場所と時間のつながりを保つ | 同じ対象の過去・別名・出典・訂正へ辿れるようにする | 最新情報で過去を消す、地域Viewごとに記録を複製する |
| ZD-07 | 人の選択を奪わない | 読むだけ、非公開、後で、取り消すという選択を残す | 公開の誘導、連続投稿の圧力、過剰な通知 |
| ZD-08 | 小さく一貫して作る | native、既存部品、共通トークンを優先する | ページ専用テーマ、顧客別UI基盤、不要な新ライブラリ |
| ZD-09 | 分類より文脈を先に見せる | 対象、場所、時間、出所、現在状態を先に理解できる | Domain Packやカテゴリ選択を理解の前提にする |
| ZD-10 | 再発見より復帰を優先する | 検索条件、地図範囲、下書き、参加対象、直前のPlaceへ戻せる | Homeへ戻して同じ対象を探し直させる |
| ZD-11 | 一画面一主題にする | 主対象・主表示を一つにし、補助操作は段階的に開示する | map、card、dashboard、sheet、CTA群を同じ強さで重ねる |
| ZD-12 | 探索から記録・参加・再訪をつなぐ | Record、Place、Program、自分の記録を同じ文脈で往復できる | 各機能を行き止まりにし、同じ情報を別画面で再入力させる |

競合時は、**安全・本人の選択 → 内容理解と目的達成 → 一貫性・アクセス可能性 → ブランド表現 → 装飾**の順で判断する。楽しさは必要だが、誤認や操作不能を対価にしない。

### 2.1 5秒テスト

主要な公開・個人画面では、初見の人が短時間で次のうち該当するものを説明できることを目標にする。

1. **何を見ているか** — Record、Place、Program、自分の記録、探索結果等の主対象。
2. **どこ・いつのことか** — 場所、日時・期間、必要な場合は時刻帯。
3. **今どの状態か** — 保存、未確認、確認済み、公開範囲、参加状態、取得失敗等。
4. **次に何ができるか** — 主要行動は原則0–1個。
5. **何が外へ出るか** — 公開・共有・参加申込み等、外部状態を変える操作では公開範囲・相手を理解できる。

全画面で5項目すべてを表示するという意味ではない。**その画面の目的に必要な問いへ、内部taxonomyを学ばず答えられるか**を受入基準とする。

5秒は初見の方向づけを検査する目安であり、測定済みの普遍的な閾値、読了期限、同意の理解、支援技術での完遂の代替ではない。長い資料や重要な条件は必要な時間を使って読める。主操作0–1個も画面の視覚的な優先順位を指し、検索結果内のリンクやキーボード操作の数を制限しない。

### 2.2 Surface budget

機能追加を、表示面・ナビゲーション・カードの追加と同義にしない。

- 原則は **primary surface 1つ + 必要時のsidecar / sheet 1つ**。作成中のみcomposer / capture flowを主面としてよい。
- 一つの状態で主要操作は0–1個、隣接する補助操作は最大2個を基本とする。詳細・稀な操作は段階的に開示する。
- スマートフォンではmap・list・detailを同じ探索文脈の表示切替として扱う。PCで地図と連動一覧を並べる構成は、一つの探索面として許容する。列の数と目的の数を混同しない。固定ナビ、文脈に応じた主操作、sheetが本文やエラーを隠さない。
- Cardは情報の所有境界・繰り返し比較に意味がある場合に使う。sectionを全部card化しない。
- 同じ専門面の機能・profile追加だけを理由に最上位ナビやdashboardを増やさない。一方、採用済みの同格専門面は固有の検索・見出し・必要なナビを持てる。全専門面を一つの撮影shellへ収容することを意味しない。[P6]
- 参加者画面へ主催・管理・配信の操作群を常設しない。責務分離を保ち、必要な管理導線はNOCOSIL側の既存面へ渡す。

### 2.3 外部プロダクトをどう参照するか — 観測と採用を分ける

参照の単位はブランド一覧ではなく利用者の仕事である。下表の「観測」は公式資料にある挙動、「採用」はZUKANの設計判断であり、効果を実測した主張ではない。横断する製品を無理に排他的な業界分類へ押し込めず、重複する仕事を一つの規則にまとめる。2026-09-20に確認した資料と保留範囲は§17に残す。

| 比較する仕事 | 一次資料で観測したこと | ZUKANに採用する規則 | 持ち込まないこと / 検証する場面 |
|---|---|---|---|
| 探索範囲を変える | iNaturalistの地図・一覧・グリッド切替、地図移動後の明示的な再検索 [C1] | 一つの検索状態を保ち、地図移動と条件適用を分ける | 生物分類や位置許可を全探索の前提にしない。戻る・位置拒否・0件を検証 |
| 気になる対象へ戻る | Google MapsのSavedはオフライン地図とは別で、共有しなければ本人用 [C2] | 保存、共有、端末downloadを別の操作・状態にする | 保存を来訪・予約・長期嗜好と推定しない。保存→再読込→解除を検証 |
| 候補を比較する | AirbnbのWishlistは対象をまとめ、日付を変えて条件を確認できる [C6] | 対象の同一性を保ち、日付・価格単位・提供条件を揃えて比べる | 検索条件の黙示保存、共同編集、投票をそのまま導入しない。古い候補の再確認を検証 |
| 当日の入口へ戻る | Peatixは購入に使ったアカウントからWebチケット・印刷へ到達できる [C8] | 参加対象と取引主体を保ち、インストールを必須にしない | ZUKANが未実装のチケット発行や入場保証を装わない。本人の申込先と戻り先を検証 |
| 自分の過去を見返す | Google PhotosはMemoriesの種類・表示・通知・非表示を本人が制御できる [C7] | 過去の再浮上は任意で止められ、本人の記録へ戻る | 顔認識基盤、自動再生、連続利用圧を導入しない。非表示と記録削除を混同しない |
| 公開された知識を読む | Wikimedia CodexはContent First、Trustworthyとprivacyを設計原則にする [C4] | 説明より対象、根拠は辿れ、訂正と出典を隠さない | 百科事典型の高密度を全画面へ適用しない。出所と対象時点を利用者が説明できるか検証 |
| 場所と時間を理解する | ArcGIS StoryMapsは本文と地図・画像を関連づけ、狭い画面では積み重ねる [C9] | 対象を固定して時点・資料を切替え、非ドラッグ操作でも比較する | スクロール乗っ取り、全画面演出、因果の断定を持ち込まない。片方の資料欠落でも読めるか検証 |
| 通信がない状態で使う | AllTrailsはSaved内のDownloadsを区別し、実際の事前download確認を案内する [C5] | 何を端末に保持できたかだけを示し、保存≠オフライン保証とする | 有料境界やナビ機能をコピーしない。cold start・期限・再認証・取得失敗を区別 |
| 意味のある確定を行う | GOV.UK Check Answersは送信前に内容を見直し、変更後は再入力せず確認へ戻る [C10] | 公開・申込の重要項目と影響だけを確認し、直す箇所へ戻れる | 通常のSavedまで毎回承認させない。取消と重複送信を検証 |

公共UIの土台はDADS / USWDSとWCAGを参照し、行政サイトの構成・語彙・権威を移植しない。[D1][U1][W1] Eventbriteの参加者journeyは補助資料として残すが、今回の直接取得が制限されたページの挙動を新しい採用根拠にしない。[C3]

**「競合にあるから採用」は理由にならない。** 再説明、再探索、入力、待ち、誤操作を減らす仮説を一つに絞り、対象の利用者journeyで確かめる。広告順位や星評価は事実の確かさではなく、書かれた出典の数も正しさの保証ではない。販売条件、推薦理由、情報の確認状態は別の意味で表示する。

## 3. デジタル庁デザインシステムから何を採用するか

参照基準は、2026-09-11に確認したデジタル庁デザインシステムβ版 **v2.18.0**。デジタル庁自身が、プラットフォーム型の基盤に各サービスのブランド・情報設計を重ねてスタイルガイドを定義する考え方を示している。[D1]

| 扱い | ZUKANでの決定 |
|---|---|
| 採用する | 読みやすさ、意味に基づく色、文字・余白の体系、予測できる操作、フォーム・エラー・フォーカスのアクセシビリティ |
| ZUKANとして定める | 深い緑を核にしたブランド、実記録中心の構成、場所と時間、記録・確認・公開・参加の表現 |
| そのまま移植しない | 行政サイトの画面構成、行政手続きの言葉、日本の住所階層、行政機関のロゴや権威表現 |
| 導入を必須にしない | Figma運用、Reactへの移行、全コンポーネントの一括導入、新しいフレームワーク |

DADS準拠部品の使用だけで、完成したサービスがアクセシブルになるとは扱わない。[D2] 実装に取り込む際は、使う部品・トークンの版と利用条件を確認し固定する。最新のupstreamへ無条件追随しない。デジタル庁の推奨・認定を受けたサービスであるかのように表示しない。

## 4. トーン＆マナーと言葉

**静か・明快・温かい・好奇心がある・誠実。** 子どもっぽくせず、行政文書のように硬くせず、AIが利用者を評価する話し方にしない。操作文は短く、読み物は内容に必要な長さを許す。

| 場面 | 基準となる表現 | 避ける表現・誤認 |
|---|---|---|
| 端末保存のみ | 「この端末に保存しました。まだ送信していません。」 | 保存先を示さない「完了しました」 |
| AIが特定できない | 「この写真だけでは分かりません。記録は保存されています。」※保存確認がある場合 | 誤った名前を埋める／失敗のたび撮り直しを強いる |
| 名前の候補 | 「AIの候補」「名前は未確認」 | 「AI認定」「正解率98%」を確認の代わりにする |
| 公開操作 | 「公開内容を確認」「公開する」 | 「次へ」の裏で公開する |
| 絞り込み0件 | 「この条件に合う記録はありません」「条件を変える」 | 読込失敗を「0件」と表示する |
| 参加の申込み | 「申込みを受け付けました。参加はまだ確定していません。」※その実状態の場合 | 受付や決済画面からの戻りだけで「参加決定」 |
| 復帰・訂正 | 「記録の続きを開く」「訂正内容を見る」 | 失敗や訂正を利用者の能力の問題として扱う |

例文は状態契約に合わせて使用し、文面を先に出して事実を追従させない。英語でも同じ意味を保つ。例: “Saved on this device. Not uploaded yet.” は公開済み・サーバー保存済みを意味しない。

見出しは対象・状態・行動を表し、単独で読んで意味が分かるものにする。「あなたの可能性を解き放つ」「シームレスな地域体験」等の抽象的な称賛で説明を代替しない。内部用語のRecord、Claim、Review、Publicationを一般利用者の操作ラベルへそのまま出さない。専門画面では説明付きで使用できる。

正式な名称・地名・人名は本人や出典の表記を尊重する。日本語の製品名は **ZUKAN**、運営主体を示す必要がある場面は **IKIMON**。既存の記録中心の公開ホーム見出し「撮ると、まちの今が図鑑になる。」は、その画面の互換仕様として維持する。他の専門面やQuiet Homeに同じコピーを強制しない。[P1][P3][P7]

## 5. ビジュアル基盤

### 5.1 配色の意味

既存の `brand.zukan-v1` の核を継承する。緑は森・生物だけを意味する分類色ではなく、ZUKANの識別色とする。写真や地域の表現まで緑色に加工しない。[P3]

以下はライトモードの規範値。追加色は本書で定める設計値であり、現行CSSの調査結果ではない。

| 役割 | 値 | 用途 |
|---|---|---|
| brand / action.primary | `#143F2E` | 主要操作・ZUKANの識別 |
| action.hover / action.active | `#0F3023` / `#0A241A` | 主要操作のhover / pressed |
| text.primary / text.secondary | `#17211B` / `#55615A` | 本文／補足。薄さで読めなくしない |
| surface.base / surface.subtle | `#FFFFFF` / `#F7F7F3` | 基本面／意味のある補助面 |
| border.control / border.decorative | `#68746C` / `#DDE2DC` | 入力等の識別に必要な境界／装飾的区切り |
| text.link / text.visited | `#0055AD` / `#663399` | 本文リンク。原則として下線を伴う |
| status.success / status.warning / status.error | `#146C43` / `#8A4B00` / `#B42318` | 検証された成功／注意／エラー。必ず文言を伴う |
| status.info | `#0055AD` | 中立的な案内。確認済みの意味には使わない |
| focus | **DADS Yellow-300 ＋ Black** | Webのキーボードフォーカス。ブランド色に置換しない |

brandとsuccessは別の意味トークンで扱う。同じ緑系でも「公開してよい」「真実である」を自動的に意味しない。AI未確認は中立表示を基本とし、危険を意味しない未確認状態を常に赤くしない。

通常の文字は背景とのコントラスト **4.5:1以上**、必要な操作境界・状態識別は **3:1以上**を満たす。装飾的な薄い区切り線を、入力欄の唯一の識別に流用しない。写真上の文字は画像ごとの明暗に依存させず、不透明な面等で担保する。数値の検証は不透明色の比較だけで終えず、実際の重なり、disabled、focus等でも行う。[D3][W1][W2]

DADSのYellow-300は公式トークンから正確に取り込む。参照名からHEX値を推測しない。実装時の二重フォーカスは内外の識別を維持し、forced-colorsではOSの高コントラスト設定を優先する。ダークモードは別テーマの全意味トークンが検証できてから提供し、反転フィルタだけで対応済みとしない。

### 5.2 書体・文字組み

| 役割 | 基準 |
|---|---|
| 書体 | 現行の読みやすいサンセリフ系を継承。日本語はNoto Sans JPまたは適切なOSフォント。言語別fallbackを持つ |
| 本文・入力・重要な操作／状態説明 | **16 CSS px / 1rem以上** |
| 補助文字 | 原則16px。短い日付・付随情報等、制約がある場合のみ14px / 0.875rem。14px未満を新規使用しない |
| 太さ | 本文400、見出し・必要な強調700を基本。ウェイト追加は用途と読込み負荷で判断 |
| 公開紹介用H1 | 既存基準のPC40–52px、スマートフォン30–34px。長文を巨大化しない |
| 通常の画面見出し | 24–32pxを出発点とし、個別画面契約と階層に合わせる。H2は22–26pxが基準 |
| 行高 | 日本語本文1.7–1.8、見出し1.3–1.45。多言語本文は原則1.5以上を出発点に実文字で検証 |
| 読み物の行長 | 日本語約35–45字、ラテン系約55–75字を目安にする。狭い端末で字数のために文字を縮めない |

DADSの16px基準と14px例外を参照するが、上記の組合せはZUKANのルールである。[D4] pxは設計時の基準値で、Web実装はrem等で利用者の文字設定を尊重する。文字の拡大を禁止しない。

日本語の禁則処理、長い地名、単語、数字と単位、混在言語を確認する。固定`<br>`、全面`nowrap`、`word-break: break-all`、overflow隠蔽、文字縮小で一枚のスクリーンショットだけを整えない。アラビア文字の連結や他言語の字形を壊す字間設定をしない。Webフォント未読込みでも内容と操作が成立すること。

### 5.3 余白・形・密度

間隔は既存の **4 / 8 / 12 / 16 / 24 / 32 / 40 / 56px** を基準にする。内容幅は最大 **1200px**。左右余白はスマートフォン20px、タブレット28px、PC40pxを基本とし、320px幅でも内容を欠かさない。[P3]

原則は、白い面、揃った文字、意味のある余白、控えめな境界。角丸は新規共通部品の標準として操作4px、コンテンツ容器8px。円形はアバターや撮影操作等、形の意味がある場合に限る。影は浮いたメニュー・ダイアログ等の重なりを説明するために使い、一覧の全カードを浮かせない。

**統一するのは判断と部品であり、全ページの密度ではない。** 読み物は読むための余白、一覧は見渡しやすさ、撮影は集中、地図は探索の連続性を優先する。全内容をカード・pill・ダッシュボードへ押し込めない。

### 5.4 構成の文法と実装トークンの境界

同じカードを全用途に使うのではなく、意味に合わせて四つの基本構成を再利用する。

| 構成 | 適する内容 | 固定する読み順 |
|---|---|---|
| 検索・比較の一覧 | 催し、求人、飲食、資料等の候補 | 対象名 → 判断に必要な事実 → 状態と出所 → 対象を開く / 保存 |
| 記録のグリッド | 写真そのものが識別に役立つ本人/公開記録 | 安全な媒体 → 日時・対象 → 確認/保存の必要な状態 |
| 読むための詳細 | Place、資料、ガイド、企画の本文 | 対象と現在の条件 → 内容 → 根拠・関連対象 |
| 作業に集中する面 | 撮影、入力、確認、申込 | 対象 → 入力/確認内容 → 保存・送信結果。探索の装飾を持ち込まない |

画像がない項目は文字と余白で成立させ、空の画像枠や関係のない写真で埋めない。文章や資料の一覧を画像グリッドに変えない。比較に必要な価格単位・日時・募集終了・注意は省略で隠さず、説明文だけを適切に短縮する。行全体の遷移と独立した保存buttonを入れ子のリンクにしない。読み物の行幅と探索一覧の幅を同じにする必要はない。

**実装との既知の差:** source `baf58895230ad9918524a250a4c66c1d73596553` の `frontendFoundation.ts` はradius 10/16/24px、focusは3pxのcurrentColorを定義する。本書の新規ZUKAN部品の4/8pxとDADS二重focusへ適合済みではない。[P9] 共有Foundationは操作・状態の再利用元、ZUKANの見た目は本書が所有する。変更する部品だけ既存の宣言元/製品スコープへ対応付け、別製品の値を一括上書きせず、後付けCSS overrideを積み上げない。この差はsource比較であり、全画面がその値で描画されると断定するものではない。

Foundationの160/220msは短い操作遷移に再利用できる。320msのenter演出は全要素に自動付与せず、待ちと見づらさを生む場合は省く。必要性のない動きを統一のためだけに増やさない。[P8][P9]

## 6. 写真・音・図・アイコン・動き

実際の記録を主役にする。自然だけでなく、建物、暮らし、文化、仕事、人の活動等が、権利と安全の範囲で自然に共存できる構成にする。少数の写真しかない状態でも、同じ画像をheroと一覧へ機械的に繰り返さない。

一覧は既存の安全な派生画像と比率を再利用し、詳細では文脈や内容を欠落なく確認できる表示を優先する。切り抜きが観察・証言の意味を変える場合は切り抜きであると分かるようにする。写真を美しく見せる処理で記録の内容を改変しない。生成画像やstock画像を、実際の地域記録・参加実績・証拠として見せない。説明図は「説明図」等と区別する。

alt、キャプション、文字起こし、サムネイル、ファイル名、EXIFにも公開範囲を適用する。非公開の正確な位置や人物情報を代替文で漏らさない。AI候補の種名を断定的なaltへ転記しない。装飾画像のaltは空、意味のある画像は文脈に応じた代替を用意する。音・動画には必要な字幕・文字起こしと操作を用意し、自動再生を基本にしない。[W1]

アイコンは既存セットを優先し、同じ意味には同じ記号とラベルを使う。絵文字を唯一の状態・操作表示にしない。音声・カメラ等のデバイス権限は必要な操作時に求め、拒否された場合の代替を持つ。

動きは状態変化を説明するものだけ。Webの短い遷移は原則150–250msを出発点にし、操作を待たせない。reduced-motionでは非必須の動きをなくす。自動カルーセル、装飾的な常時アニメーション、スクロールの乗っ取り、通知を促す点滅は採用しない。

## 7. 情報設計と画面をまたぐ一貫性

既存App Experience [P3] のナビはそのapp shellの契約であり、全ての同格専門面に必須の親shellではない。[P6] Quiet Home/Savedの範囲は[P7]で整合する。ロゴ・基本操作・本人入口・公開範囲の理解は共通にし、専門面に合う検索と主行動は維持する。

| 責務 | 利用者にとっての意味 |
|---|---|
| 横断ホーム | 選んだ文脈で探す入口と、保存した対象等への安定した復帰。一般のタスク受信箱ではない |
| 専門面 | 飲食、仕事、催し、自然等、その目的から直接入って用を済ませられる |
| 記録 | 自分の記録と、閲覧を許可された公開記録を明確に分けて見返す |
| 場所 | 同じ対象の現在・過去・資料・活動を場所から辿る |
| 参加 | 企画を比較し、本人の正確な参加対象・状態・当日の案内へ戻る |
| 保存したもの | 本人が保存した対象への私的な参照。記録の原本や予約台帳とは別 |
| 自分 | 本人のidentity・事実に基づく小さな要約・必要な設定。各アーカイブを重複させない [P11] |
| 撮る / 残す | 必要な文脈から始められる行動。全専門面で必ず最も強いCTAになるわけではない |

既存app shellのPCヘッダー、1160px以下の5項目下部ナビと本人入口は、変更対象として明示するまで維持する。今回の規則だけでタブ数・breakpoint・公開URLを変更しない。機能の存在が確認できない入口を出さない。本人の推定personaに応じて主要ナビを移動しない。

### 7.1 Home — 文脈を先に置き、棚を増やし続けない

読み順は **選択中の地域/範囲と目的 → 検索 → 意味が揃った内容 → Saved・自分の記録へ戻る入口**。差し迫った安全・復旧・参加変更が実在するときだけ小さく割り込ませる。画面を開くたび巨大な宣伝heroや完了済みonboardingを再表示しない。

Quiet Homeの初期上限は**3棚×各3件のpreview**。[P7] これは必ず9件埋めるノルマでも、全専門一覧の件数上限でもない。各棚は一つの目的を持ち、続きは同じ条件の一覧へ送る。データが少なければ棚を減らす。私的Savedと公開の発見を同じ「あなたへのおすすめ」に混ぜない。掲載権限があってもHomeへの媒体promotion権限は別に確認する。

場所未指定なら地名/対象名で始められる。近くを本人が選んだ時だけ位置を求める。旅先を選んだ人をGPSの居場所へ戻さず、オンラインの対象に架空の地点を付けない。ログイン後も専門面のdeep linkを保つ。

### 7.2 Search / Map — 一つの探索を、違う見方で表示する

読み順は **検索語・範囲・日付 → 適用中の条件 → 件数の意味/取得状態 → 一覧または地図 → 選択対象の短い詳細**。一覧は意味のある既定表示であり、WebGLや位置許可を閲覧の前提にしない。

検索語、選択Area/到達範囲、日付と時刻帯、明示filter、並び順、表示方法、読込位置、選択対象を同じ探索の状態として扱う。既存URL/state機構を使い、新しい検索エンジンや共通storeをこの規則だけで作らない。安全な公開条件だけURLへ反映し、私的な検索語・非公開座標・Saved識別子を共有URLやanalyticsへ漏らさない。

地図のpan/zoomで対象を勝手に選び直さない。範囲を変えた場合は「この範囲で検索」等で再適用でき、結果とpinが同じ適用範囲を指す。[C1] 総件数、読込済み件数、画面内の件数は別に表示する。clusterの数を地域全体の記録数と呼ばない。新しい検索中は前の結果と現在の処理対象を識別し、遅れた旧応答で新しい結果を上書きしない。

詳細から戻ると条件・位置・選択対象を復元し、filter変更は古い選択や件数を適切に無効化する。公開権限が失われた対象は復元しない。失敗を0件に変換せず、以前の結果が表示されるならその範囲と古さが分かる。修正案は「日付を広げる」等の一条件ずつにし、勝手に条件を広げて一致を装わない。

### 7.3 詳細の共通順序と、種類ごとの違い

詳細は **対象の同定 → 判断に必要な現在の事実 → 主行動 → 本文/媒体 → 出所・変更/関連対象** を基本とする。媒体が主対象のRecordは媒体を先に置ける。安全・取消・掲載終了は本文や販促より先。出典を読むためにアカウントやAI会話を必須にしない。

| 対象 | 先に分かるべきこと | 主行動の候補 / 誤認させないこと |
|---|---|---|
| Record | 媒体、記録した時点、場所の公開精度、名前等の確認範囲 | 所有者は続きを編集/公開内容確認、閲覧者は読む/場所へ。全Recordに公開CTAを強制しない |
| Place | 同じ場所のidentity・別名、現在の有効な情報、時点と出所 | 現在/過去を読む、保存、関係する記録へ。古い写真を今の営業状態と扱わない |
| Program / Event | 対象の回・部分、現地日時、場所/online、主催者、費用、申込先、変更状態 | 実状態に合う「参加方法を確認」「この回に申し込む」等。親企画の申込と個別回の確定を混ぜない [P10] |
| Food / Stay / Experience | 店舗/支店と提供者、提供日時・条件、価格の単位、情報時点、予約の担当者 | 営業/予約条件を確認。現在営業中・空席ありを過去資料から生成しない |
| Job / Learning | 募集/提供主体、仕事内容/学習内容、場所またはremote条件、報酬/費用の単位、対象条件・期限 | 条件や応募/申込先を確認。保存を応募済みにせず、言語から対象国籍を推定しない |
| Offer | 対象、利用できる期間と場所、必要条件、発行/利用の担当者 | 条件確認、実装済みの場合のみ取得/利用。保存・発券・使用済みは別 |
| Article / Publication | 誰の発表/編集か、対象時点、発行/改訂日、原資料と利用権 | 読む、出典・関連対象へ。発表者の主張を独立検証済みと見せない |

この表は型ごとの表示契約であり、未実装の求人・予約・クーポン機能を本番に出す指示ではない。unknownな型は限定された資料/記事として示し、検証済みの専用取引を装わない。[P6]

### 7.4 Placeと時系列 — 過去を消さず、差を読み取れる

同じPlace/対象を固定し、二つの時点・資料の比較ではそれぞれの日時、撮影/発行主体、位置や比較条件の限界を示す。横並びが読めない幅では縦に積み、見出しと順序を保持する。[C9] sliderや地図dragだけを唯一の操作にせず、時点選択や前/次等の単一pointer・keyboard操作も用意する。[W7]

資料が一つなら一つで成立させ、架空の過去/現在を補わない。記録がないことを「生物がいない」「変化がない」と表示しない。写真間の差を測定済みの傾向・原因・改善効果へ飛躍させない。明示的な再記録は同じ対象へ戻せるが、正確な非公開位置や危険な立入を誘導しない。

### 7.5 Saved・記録・自分 — 戻り先を一か所ずつにする

Savedは既存対象への私的な関係、記録は本人の原記録、自分はidentityと設定である。三つの面へ同じカード一覧を複製しない。Saved入口を設定の奥へ隠さず、既存shellの許可された入口から安定して到達させる。新たなbottom tabはこの規則だけでは追加しない。[P7][P11]

通常は一回の保存操作でよく、コレクション名・旅程・AI整理を必須にしない。保存後は押した対象に状態を返し、再読込でも一致する。解除は同じ関係へ適用し、重複クリックや遅れた再試行で復活しない。書込み未確定の楽観表示は「保存中」と最終状態を区別する。[P7]

保存した価格・日時等は当時のsnapshotであり、現在も同条件という保証ではない。再訪時は公開対象の現在の状態を確認する。公開撤回や権限喪失を検出したら再配信せず、理由の開示範囲を守った「現在は表示できません」等へ戻す。本人が独立して書いた私的メモの扱いと分ける。

### 7.6 Capture — 入力を減らし、保存範囲を明示する

入口の対象・Program・選択時点は引き継ぐが、撮影時刻と予定日時を混同しない。media/音声/文字のうち実装済みの方法を使い、カメラ拒否時は選択・文字等の利用可能な代替へ進める。分類、AI成功、公開同意を私的保存の必須条件にしない。

読み順は **取り込んだ内容 → その対象・日時/場所の確認 → 保存状態 → 任意の整理・公開**。現場で長い説明文、全metadata、権利選択肢を同時に展開しない。既知情報の再入力を避け、曖昧なものは未確認のまま保持する。離脱・再認証後は同じ下書きへ戻し、同じ原記録を作り直して重複させない。

### 7.7 参加と外部引継ぎ — 探す段階と当日を分ける

探索は日時・条件の比較、申込は対象範囲と結果、当日は変更と集合/接続方法を優先する。申込済みの人を一般のイベント一覧から探し直させない。[P10] アプリを入れなくても使えるWebの戻り先を優先する。[C8]

外部予約・応募へ移る前に担当サービスと対象を示す。リンクのクリック、外部画面からの復帰、ZUKANのSavedを、予約/応募/決済の成功とみなさない。結果が読めない場合は確認先を示し、重複購入や再応募を自動誘導しない。カレンダー等へのhandoffは既存native機構を優先し、独自チケットwalletやcalendarを新設しない。

### 7.8 Public reading — QR・検索から一つの内容へ直接入る

検索、案内板QR、共有、翻訳ページから来た人にHomeやログインを先に通らせない。対象の説明、出典、現在の有効性、次に辿れる関連対象が読めれば価値がある。原資料全文の再掲権がなければ出典・許可された要約・リンクを返す。未対応言語を翻訳済みと装わず、原文へ戻れる。

求人、店舗、案内板、人物インタビュー、発表資料を生物Recordとして表示しない。一般の読み物にAIチャットや投稿ボタンを必ず置かない。媒体や専門面が変わっても、対象と版・公開範囲の意味は維持する。

## 8. 基本部品と操作の契約

新しいコンポーネント基盤を作る前に、native HTML、現行の共有部品、DADSの適合する部品を比較する。現行のAlpine/Tailwind/MapLibre等を、参照資料に合わせるためだけに置換しない。[P4][W3]

| 部品・場面 | 必須の性質 |
|---|---|
| ボタン／リンク | 実行はbutton、移動はlink。具体的なラベル。primary・secondary・destructiveを区別。hover以外でも意味が分かる |
| タッチ操作 | 操作領域44×44 CSS px以上をZUKANの原則とする。独立した撮影主操作は56px以上を出発点にし、既存の大きい領域を縮めない |
| フォーム | 常に読めるラベル、必要性の理由、任意／必須、書式の例。placeholderだけをラベルにしない。必須入力は目的達成に必要なものだけ |
| エラー | 対象、理由、直す方法を近接表示。複数項目は必要に応じ要約から辿れる。入力を保持し、色だけで伝えない |
| disabled／権限不足 | 押せない理由と可能な代替を示す。不可視の操作だけに説明を置かない。無権限の内容そのものは開示しない |
| dialog／sheet | 必要な場合だけ。名前、閉じ方、キーボード操作、focus管理と復帰。閉じた結果失う情報は事前に分かる |
| 検索・絞り込み | 適用条件、件数の範囲、解除方法が分かる。取得不能と0件を区別し、読込で元の結果を不必要に消さない |
| 一覧・カード | 題名・媒体・時期・場所等、見分けるための情報を優先。カード内リンクや選択を曖昧に多重化しない |
| 表・比較・タイムライン | 見出し・単位・日時・対象範囲を示す。二時点の差を直ちに因果関係と表現しない。視覚操作以外の読取方法を持つ |
| 通知・成功表示 | 実際に成立した範囲を知らせる。消えるtoastだけに重要な結果や復旧手段を残さない |

関連する部品はdefault、hover、focus、pressed、selected、disabled、loading、empty、partial、error、deniedを必要な範囲で持つ。全ての部品に不要な状態を機械的に実装しない。処理中の重複実行を防いでも、失敗からの再試行やキャンセルを奪わない。

44pxはZUKANの使いやすさの基準であり、WCAG 2.2 AAのTarget Size Minimumが一律44pxを要求するという意味ではない。同項の基準は24pxと定められた例外・間隔条件である。[W4] 本文内リンク等の例外は、その基準と実際の操作性に沿って扱う。

### 8.1 検索・比較・入力の具体的な操作

検索はラベルと送信操作を持ち、IME変換中のEnterを検索確定と扱わない。入力中に対象やfocusを勝手に変えない。候補がなくても手入力を続けられる。filterは適用済みと編集中を区別し、適用/解除後の状態が読める。countは必要な区切りで控えめに通知し、文字入力のたび読み上げ続けない。

比較はまず同じ一覧の事実を揃える。専用compare画面はそれで不足する仕事にだけ追加する。価格は通貨、税込/別途費用の分かる範囲、人数/泊/時間等の単位を保ち、unknownを0円にしない。jobの月給と時給、宿泊と一人分の料金等を同じ数字として並べない。異なる型の対象は目的別のまとまりで読むことができ、無理に一つの総合scoreで順位づけしない。

### 8.2 重要な確定と、軽い操作を分ける

現在の承認境界で確認が必要な公開・申込等では、**対象/相手 → 外へ出る項目と媒体 → 必要な条件・費用 → 確定操作**が分かる確認を使う。変更した項目から戻っても、他の入力をやり直させない。[C10] 私的なSavedの追加、検索、表示切替に儀式的な確認画面を追加しない。同じ範囲で有効な継続承認がある処理へ、本書だけを理由に毎回の人間承認を追加しない。

送信後の結果は実際のread-backで表し、通信切断で結果不明なら「確認中/確認できません」とする。二重効果の可能性がある場合、同じ操作の状態を確認してから再実行する。toastは補助であり、重要な受付番号・状態・戻り先は対象の面に残す。

### 8.3 Sheet・固定要素・戻る

detailやfilterのsheetは一度に一つを基本とし、別のsheetを重ねるより内容を切替える。modalとして実装する場合は背面操作を止め、閉じる、Escape、focusの閉じ込め/復帰を意味に合わせる。非modalの連動paneにmodalの動作を誤って付けない。

固定ナビと主CTAとkeyboardを縦に積んで入力欄を隠さない。safe-areaと可視viewportを考慮し、長い条件は内容としてscrollできる。WCAGの最低要件はfocus対象が完全に隠れないことだが、ZUKANは操作対象と必要な説明を読める位置へ戻すことを目標にする。[W8] ブラウザの戻る、sheetを閉じる、編集取消の戻り先を区別する。

## 9. 状態の正直さ — 一つの「完了」に潰さない

下表は既存の状態を表示する責務の整理であり、新しい共通状態エンジンやDBの要求ではない。各軸は独立しており、一本の進捗ステップにはまとめない。[P2][P3]

| 軸 | 区別する意味 | 禁止する短絡 |
|---|---|---|
| 原記録の保存 | 端末下書き、送信中、サーバー保存、一部保存、復旧可能な失敗 | 端末保存＝サーバー保存／永久に消えない |
| Savedの関係 | 保存中、保存された参照、解除中、解除、対象が利用不可 | bookmark＝原本複製／download／予約／応募 |
| AI処理 | 未処理、処理中、候補あり、判断できない、処理失敗 | 候補がある＝人が確認した |
| 名前・内容の確認 | 対象ごとの未確認、本人の確認、共同確認、意見の不一致、訂正 | 一つの対象の確認＝記録全体の真実性 |
| 閲覧範囲 | 本人、許可された相手、公開等の実際の範囲 | アカウントを持つ＝公開同意がある |
| 公開・配信 | 権利条件、配信可能、公開操作、ZUKAN掲載、外部掲載・受領の証拠 | eligible＝掲載済み／自分のURL＝外部掲載実績 |
| 参加 | 保存した予定、申込み、受付、確定、変更、取消 | 興味・予定・決済画面からの戻り＝参加確定 |

内容の近くには判断に必要な最小限を出し、詳しい出典・変更履歴へ辿れるようにする。全ての内部状態をバッジで常時並べない。誰が、何を、いつ、どの根拠で確認したのかを必要な粒度で読めるようにする。

AIは候補を返す。未確認を隠してもっともらしい本文を作らない。意見の不一致を単なる「未確認」に戻さず、AIが作った説明を人のReview理由として表示しない。確認や再利用を行わない普通の閲覧まで、一律の人間承認待ちにしない。

### 9.1 根拠と鮮度は、対象となる事実の近くに置く

一枚のbadgeでページ全体を「確認済み」にしない。名前の同定、開催予定、営業条件、資料の発行、掲載権は違う確認である。まず判断に必要な状態と日付を短く示し、次の層で「誰が何を確かめたか」、さらに原資料/版/訂正へ辿れるようにする。[C4]

記録した日時、内容の有効期間、発表/更新日時、ZUKANで取得・確認した日時を区別する。今日取得した古い営業時間を「本日確認済みの営業」と見せない。重要条件が古い/不明なら、その事実と確認先を主操作の前に出す。掲載終了、予定変更、資料更新後の翻訳未更新、情報源間の不一致を同じ「情報なし」にしない。

### 9.2 AIは文脈を補助し、状態を確定しない

通常の検索、filter、保存、並べ替え、元資料の表示をLLM待ちにしない。自然文の探索や要約が役立つときも、解釈した地域・日付・条件を修正できる形で示す。AIが使えない時は通常検索/本文/保存済み原記録へ戻れる。新しいmodel/providerを採用してもこの契約は変えない。

候補や要約の隣に根拠と未確認範囲を保ち、元の記録を置換しない。本文の生成が成功しても、公開・予約・同定・通報等の成功を作らない。外部送信を伴うAI利用は既存の権利と費用制御に従い、裏で常時カメラ/位置/音声を取得する許可を推定しない。

## 10. 世界対応は翻訳機能ではなく設計条件

**操作と信頼の基準は共通にし、地域の表現は均質化しない。** 日本の住所、英語の語順、常時ネット接続、個人所有スマホを唯一の前提にしない。[P5][W5]

| 領域 | 必須の設計 |
|---|---|
| 言語と地域 | UI言語、コンテンツ言語、地域設定、時刻帯を区別。BCP 47等の標準言語タグを使い、言語選択は自称表記。国旗を言語の代わりにしない |
| 原文と翻訳 | 原文・出典・機械翻訳／人の翻訳を区別。表示言語を変えても原文や対象のIdentityを上書きしない |
| 長さと方向 | 実訳と長文・疑似翻訳で検証。1.5–2倍長のテキストは試験条件として使う。RTL、双方向混在、改行しない長い文字列に対応 |
| 入力・氏名 | UTF-8、IME、アクセント、多様な文字体系を扱う。全世界で姓／名二分割や日本の住所・電話形式を必須にしない |
| 日時・数値 | locale対応の書式・複数形・単位を使い、文を断片連結しない。参加判断で重要な開催地時刻帯を明示し、観察時刻と投稿日を区別 |
| 場所 | 現地名、別名、歴史名、地理・文化圏を必要に応じ併記。日本式行政階層を世界共通フォームにしない |
| 確かさ | 日付不明・概算・位置のぼかし・境界や名称の異なる出典を表現。架空の正確な日付や地点で埋めない |
| ローカル適合 | 地域ごとの権利、子ども・代理参加、価格・支払い、緊急連絡等の条件は現地適用を確認。翻訳だけで法的有効性を保証しない |

地域・言語・市場・法域・利用資格は別の文脈である。開催地の時刻と利用者の時刻を併記する場合はラベルを付け、日付のみ/期間未定を勝手に深夜0時へ変換しない。翻訳が原文より古い時は重要な変更を隠さない。言語切替で対象のID・日付・保存関係を別物にせず、提供のない言語/地域は実際の選択肢へ案内する。[P6][P7]

CSSは論理プロパティを優先する。RTLで進む／戻る等の方向性は適切に反映するが、ロゴ、地図、写真まで左右反転しない。日時の元精度を守り、日付しか分からない記録へ時刻や時刻帯を捏造しない。歴史的・係争的な地名や境界は、出典と対象時点を示し、UIの都合で唯一の解釈に統一しない。

**対応言語として掲げる条件:** その言語で提供する主要な閲覧・記録・参加導線に加え、エラー、保存状態、公開範囲、訂正、支援窓口が理解可能であること。未翻訳・未対応の範囲は明示する。重要な同意・安全説明は適切な言語レビューを通し、機械翻訳だけで新しい同意を成立させない。試験用localeの存在を、その国への正式提供・法令適合と扱わない。

## 11. アクセシビリティの受入基準

Webは **WCAG 2.2 Level AA** を目標適合水準とし、適用される成功基準全体を対象にする。下記はZUKANで見落としやすい重点項目であり、WCAGの代替チェックリストではない。[W1]

| 重点 | 必須の結果 |
|---|---|
| キーボードと支援技術 | 主要な閲覧・記録・参加をキーボードで操作でき、名前・役割・状態・読上げ順が適切。native HTMLを優先し、ARIAだけで動作を装わない |
| フォーカス | 常に見つけられ、固定ナビやsheetで隠れない。閉じる／戻る後の行き先が予測できる |
| 拡大・リフロー | 文字200%拡大、320 CSS px相当のリフロー、400%ズームで主要内容と操作を失わない。二次元性が必要な地図等の例外をページ全体へ広げない |
| 入力と認証 | ラベル・エラー説明・補助入力を持ち、pasteやpassword managerを不必要に禁止しない。暗記・パズルを唯一の認証手段にしない |
| 見る・聞く・動かす | 色・音・hover・ドラッグ・動きだけに依存しない。代替テキスト、字幕、必要な非ドラッグ操作を用意する |
| 時間と集中 | 不必要な制限時間や自動更新を避ける。大切な入力・確認の途中に通知や動きで割り込まない |

コントラスト、ターゲットサイズ、reflowの根拠は[D3][W2][W4][W6]。native-firstとARIAの注意点は[W3]。自動検査に加え、影響する操作を実際に使って確かめる。設計トークンの数値、チェックツールの合格、スクリーンショットだけで、全サービスのWCAG適合や障害当事者による検証を主張しない。

## 12. 屋外・低速回線・失敗時の体験

屋外の日差し、片手、移動の合間、古い端末、通信制約を通常の設計条件に含める。ただし歩きながらの撮影・危険な接近を促さない。

記録は、保存の事実を確認してから保存済みと表示する。AI完了や地図読込みを保存の条件にしない。同期・認証・権限の問題がある場合、保存済みの範囲と復旧方法を示す。端末の保存はブラウザやOSの制約を受けるため、永久保存を約束しない。オフライン・再起動・再認証後の復帰は、実機で成立した範囲だけを提供機能として説明する。

画像は用途に合う安全なサイズ・形式を配り、寸法を予約する。画面外の画像や重い地図・動画を遅延し、必要な最初の内容を遅延させない。Webフォントや装飾ライブラリの失敗で読む・入力することが止まらないようにする。失敗に対して無限retryや永久spinnerで通信・電池を消費しない。

Webの性能目標は、実利用の75パーセンタイルで **LCP ≤ 2.5秒、INP ≤ 200ms、CLS ≤ 0.1**。端末区分等を分けて評価する。[F1] これは採用する目標であり、ZUKANで測定済みの数値ではない。実利用データが足りない場合は未評価とし、ラボ値を実利用値として扱わない。写真保存や同期の所要時間は、これらの表示指標と別に計測する。

## 13. 公開・参加・NOCOSILとの接点

公開前に、誰に何が見えるか、名前・場所・媒体がどう扱われるかを理解できるようにする。公開可否、ホームへの紹介、外部配信、素材の再利用を同じ同意にまとめない。既存の権利基盤を使い、ルールブックを理由に新しい権利や代理権を作らない。

子ども、希少種、個人宅、文化的に配慮すべき場所等の安全を、表示・検索・地図・共有カード・export・メディア派生物まで一貫して扱う。公開を解除しても外部コピーの即時消去を保証しない。撤回できる範囲、対応中、外部への依頼等の実状態を伝える。

参加画面は、内容、日時・時刻帯、場所／オンライン、参加する部分、条件・費用、申込み先、変更状態を判断できることを優先する。部分参加の希望は承認の事実がなければ希望のまま示す。外部サービスを使う場合も、どこに移り、誰との手続きか分かるようにする。[P3]

**製品の差は、public/privateという一語や機能の有無ではなく、利用者のjourneyと説明責任で決める。** ZUKANは探索・比較・保存・記録・参加を単独で完遂できる。NOCOSILは許可された情報と本人の考え・生活・仕事の継続を担い、両方にcapture・検索・AI・公開があり得る。[P6][P7]

参加者画面に第二の運営dashboardは増やさず、新しい主催・管理・制作の操作は採用済みのNOCOSIL境界へつなぐ。既存の開催・再開催は代替体験の受入まで維持し、いきなり利用不能や有料必須にしない。一般閲覧やZUKANの基本利用にNOCOSIL登録を強制しない。[P3][P10]

NOCOSILへの任意連携は、双方の本人確認と明示した範囲を持ち、既定の行先は本人のPersonal領域とする。今開いている勤務先WorkspaceへSavedや求職活動を自動転記しない。連携失敗はZUKANの保存失敗ではなく、解除は新しい配信の停止と過去の保持/削除を分けて説明する。未接続のconnectorを「同期済み」と表示しない。[P7]

連携時は、製品を移ること、引き継ぐ対象・操作・公開範囲を示す。同じ運営会社・アカウントの存在を、private情報を公開する許可に読み替えない。組織の発表内容は発表者の主張として扱い、独立検証済みの事実に見せない。

## 14. 良い体験だったかを何で判断するか

**主指標は、意図した行動が成立し、利用者が結果を理解できること。** 数だけを増やすランキングや人の価値のスコアは作らない。

| 評価する結果 | 確かめるもの |
|---|---|
| 初回理解 | 記録の内容・時期・場所が理解でき、次に何ができるか分かる |
| 記録の信頼性 | 保存先の誤認、保存したつもりの消失、再送での重複、復帰不能がない |
| 確認の理解 | AI候補・人の確認・公開範囲を取り違えない。根拠と訂正先へ辿れる |
| 参加の明快さ | 対象の回・部分・日時・費用・申込状態を取り違えない |
| 継続価値 | 過去の記録、場所の変化、参加の続きを見つけ、戻れる |
| 包摂性 | 言語・端末・入力手段・支援技術の違いで主要行動が不必要に排除されない |

利用者テストでは、説明前に本人の言葉で「今どの状態か」を聞く。クリック成功だけで理解を推測しない。初めて使う人と継続利用者、対象locale、支援技術利用者等の実際の文脈で確かめる。全地域に通用する効果を少数の日本語テストから一般化しない。[U1]

変更の効果は、同じ仕事で必要だった手動操作・探し直し・再入力・待ちと、結果の取り違えを比較する。例えば「保存した企画の当日案内へ戻る」一往復の所要時間と失敗、「検索→詳細→戻る」で失われた条件、「公開確認」で誤認した範囲を測る。数値のbaselineがなければ未測定とし、5秒テストや画面の好みだけで改善率を作らない。

既存の計測・サポート・WorkのEvidenceを使い、ルールブックのための別分析基盤を作らない。検証に必要な最小データに留め、内容や正確な座標を無断でanalyticsへ送らない。

## 15. 実装とレビューへ渡す受入条件

まず影響範囲を選び、該当する条件だけを検証する。基盤を初めて実装・更新するときには代表的な広さと状態を確認し、局所的な文章修正へ全行列を毎回要求しない。[G1]

### 10問のデザイン受入

変更した面について、該当する問いに短く答えられない場合は実装前または受入前に設計を戻す。

1. ZUKAN固有のtaxonomy・内部用語を知らなくても主対象を理解できるか。
2. 5秒で、対象・場所/時間・現在状態・次の一歩のうち必要なものが分かるか。
3. 実際のRecord、Place、Program、写真、資料が、UI chromeやAI演出より主役になっているか。
4. 同じ状態で主要操作が複数競合していないか。
5. 戻る・再開で検索条件、地図範囲、下書き、参加対象等の文脈を不必要に失わないか。
6. 0件、未確認、取得失敗、権限不足、非公開、終了を同じ見た目・文言に潰していないか。
7. 地図、AI、常時通信が使えなくても主要な閲覧・保存・復帰が成立するか。
8. private / public、AI候補 / 人の確認、希望 / 申込み / 承認等を誤認させないか。
9. 320pxを含む影響幅で、固定要素・長文・日本語改行・keyboard / safe-areaが主要内容を隠さないか。
10. 同じ仕事のためにnav、dashboard、card層を重複させず、同格専門面の目的まで一つの親shellへ押し込めていないか。

### 代表シナリオ

| ID | シナリオ | 受入条件 |
|---|---|---|
| QA-01 | 登録前に記録を読む | 内容・日時・場所・出典の意味が分かり、登録を強制されず戻れる |
| QA-02 | 撮影後に通信／AIが失敗する | 実際の保存範囲が分かり、既存の復旧方式で続けられる。確認前の完了表示がない |
| QA-03 | 私的な記録を確認・公開する | AI候補、人の確認、公開内容、実際の掲載を取り違えず、非公開情報が漏れない |
| QA-04 | 場所を検索し、二時点を見る | 地図なしでも到達でき、時刻・精度・不明点を保ち、戻って探索を継続できる |
| QA-05 | 企画を選び参加へ戻る | 対象の日時・範囲・条件・申込先・現在状態が一意に分かる |
| QA-06 | 長い地名・翻訳・拡大・キーボード | 文字や操作が欠けず、順序・focus・入力・復帰が成立する |
| QA-07 | 専門面のdeep link→必要な認証→戻る | 同じ対象・日付・目的へ戻り、Homeや撮影を経由し直さない |
| QA-08 | 探索→map/list切替→詳細→戻る | 条件・地図範囲・読込位置が保たれ、遅れた応答/地図失敗で別条件に変わらない |
| QA-09 | 保存→再読込→解除→遅れたretry | 同じ私的関係が一つで、解除が復活せず、保存中/成功/失敗を区別する |
| QA-10 | 保存した対象が変更・終了・非公開になる | 現在の条件と確認先へ戻り、古い予約/価格/公開可能性を保証しない |
| QA-11 | 明示した旅先/日付、remote、位置拒否 | GPSで上書きせず、架空Placeを要求せず、公開検索が使える |
| QA-12 | 外部申込後の結果不明、確認項目の編集 | 戻っただけで成功にせず、同じ対象の確認先へ返す。既知項目の再入力や重複送信を強いない |
| QA-13 | 二時点の比較、資料が一つ/時刻が不明 | 非ドラッグでも読み比べられ、欠測や因果を捏造しない |
| QA-14 | 任意連携が未接続・失敗・解除 | ZUKAN単独の保存は成立し、本人の私的情報を勤務先等へ暗黙転記しない |

画面基盤の代表幅は **320 / 375 / 768 / 1280 / 1440 CSS px** と、影響するレイアウト境界。既存ナビ変更時は **1160 / 1161px** も確認する。タブレット縦横、画面キーボード、safe-area、長文、0件、取得失敗、一部取得、権限不足、非公開のみ等から、変更が影響するものを選ぶ。新しい対応localeではLTR・RTL等の実際の必要条件を加える。

権利漏洩、保存の虚偽、参加・公開の誤認、主要操作不能は受入不可。軽微な見た目の例外は、理由・影響・解消条件を同じPR／Workへ記す。ユーザーの権利や安全を「デザイン例外」で緩めない。

### 最小の引き継ぎ

```text
Source: DESIGN.md@<exact SHA> + 対象画面の既存契約 + current source
Delta: 今回変える体験・部品・文言。維持する意味と対象外。
Done: 該当するZD/QA、実データに近い状態と幅、結果・根拠・未検証境界。
```

デザイナー／Noahが構成・言葉・色・状態・レスポンシブ上の重要判断を解決してから実装へ渡す。実装者は未解決の権利・製品判断を好みで埋めない。レンダーを見ていなければ視覚受入は未検証、保存や参加を使っていなければ体験成立は未検証とする。検証量を品質そのものにしない。

## 16. 運用・変更・他の表現物

正本はこのGitファイル一つ。Figma、HTML/PDF、スクリーンショット、プロンプト、実装トークンは参照・派生物であり、それぞれに別のルールを増やさない。実装トークン化するときは本書の意味と値を対応付け、同じ責務のトークンをページごとに作らない。必要性のないトークン生成器・独立したデザイン用サービスは作らない。

外部部品は採用した範囲・版・ライセンスを既存の依存管理へ残す。コード、フォント、アイコン、写真、ロゴの利用条件を一括で同じと扱わない。公式ZUKANのロゴ資産を使い、画像生成でロゴを描き直したり、変形・独自配色・疑似ロゴを作ったりしない。[P3]

改定は通常のPRで行い、変更理由、影響する規則、旧規則の扱い、確認した根拠を残す。語句整理はpatch、互換的な補足はminor、重要な意味・共通体験の変更はmajorとして扱う。規則の正式な訳を作る場合は版を対応させ、意味の相違は本書の改定で解決する。

### v1.2.0での限定的な訂正

v1.1の「全画面で撮影が主行動」「全専門面で同じHome/ナビ」「ZUKAN=public、NOCOSIL=privateのみ」と読める部分を、既に採用済みの[P6][P7]へ整合した。原則数を増やすのでなく、8つの画面レシピ、型別の比較情報、Saved/原記録/downloadの分離、復帰・情報更新・AI失敗・確認操作の受入を具体化した。5秒テストは方向づけのheuristicとして限定し、過去の参照日付と今回の再確認を区別した。

最初に全面改修する必要はない。以後の新規・変更対象は本書へ合わせ、未変更部分が既に適合するとみなさない。既存ページの改善や優先順位付けは、この正本を用いる別のWorkとして行う。**本書の制定によって、その改修・新機能・追加言語の実装や本番反映を自動開始しない。**

## 17. 根拠と参照版

### 制定時の内部正本 — 歴史的な参照

- [P1] `yamaki0102/ikimon-business-strategy@bd118c89d62b48707f19259341f05c3a094b566f` — `strategy/zukan-service-definition-master.md`。目的・製品名・地域の広さ。
- [P2] `yamaki0102/ikimon-platform@05e0f5b33acb2f4bf335dc75b40680686dfeacf0` — `docs/spec/zukan-product-architecture/SPEC.md`。製品責務・Record/Claim/Review/Publicationの分離。
- [P3] 同じ製品source — `docs/spec/zukan-app-experience/ZUKAN_APP_EXPERIENCE_V1.md`。ナビゲーション、既存視覚方針、§16の状態・§17の責務分離。blob `01a41b2d9ae5b7bb949469e2398b485d75a8490e`。
- [P4] 同じ製品source — `AGENTS.md`。既存実装、権限、変更に比例した検証。
- [P5] [P1]と同じ戦略source — `strategy/zukan-global-place-graph-principles.md`。Place、時間、多言語、非固定行政階層。
- [G1] `yamaki0102/all-projects-management@fcbb20c75ffdf690220a21889d9f08c57ef6795d` — `operations/ai_os/noah_operating_contract.md`、`operations/ai_os/design_quality_standard.md`。判断・正本・デザイン品質・最小十分な検証。

この一覧は制定時の出典。今後の実装はcurrent canonicalとの変更差分を確認する。記載した過去SHAを永続的な実装着手sourceに固定しない。

### v1.2でfresh readした整合根拠 — 2026-09-20 JST

製品sourceの比較基準は `baf58895230ad9918524a250a4c66c1d73596553`。DESIGN旧blobは `b2867dea083575fcbad03447df1e7959897e6cb9`。Product Architecture blob `d84080fd1d3573345dcf515c872e46525525b0ca`、App Experience blob `01a41b2d9ae5b7bb949469e2398b485d75a8490e`、AGENTS blob `903650a938812401c5592baa06617e80da7fb328`を照合した。これはsourceの参照identityで、runtime適合の証明ではない。

- [P6] management `operations/decisions/2026-09-13-zukan-public-portal-network-and-nocosil-distribution-v1.md` revision 2、blob `e9ef447f93ac259c0eba5f9fccf03a7d1bbb0e88` — peer platform、URL/言語/地域/公開主体。今回§0–4冒頭を確認。
- [P7] product `docs/spec/zukan-app-experience/QUIET_HOME_SAVED_CONTINUITY_V1.md`、blob `d1c56512611555873197515772d686866a88f54c` — Home、3×3初期上限、私的Saved、任意連携、対象範囲のsupersession。既存Work `ZUKAN-HOME-SAVED-CONTINUITY-20260917`は同契約の実装継続先であり、本書の更新で完了を書き換えない。
- [P8] product `docs/frontend-foundation/README.md`、blob `74768712270c823cb8f8efc32573aaccd20b0505` — 共通操作と製品固有表現の責務。
- [P9] product `platform_v2/src/ui/frontendFoundation.ts`、blob `5c6a157cc3ca3970919ef8277f0a4c1b21cf8621` — 現在の共有sourceトークン。DOM/全画面の適合証明ではない。
- [P10] product `docs/spec/zukan-app-experience/PARTICIPATION_EXPERIENCE_V1.md`、blob `2096b42053e248a5039a784a040c86ec4dc9381b` — 今回§7.3–12の参加・外部handoff・当日・記録を確認。実装時はcurrent sourceを再確認。
- [P11] product `docs/spec/zukan-app-experience/MY_PAGE_V2.md` v2.1、blob `7870d74223155cd7fc9e2d5b351d09e9d14622b3` — 自分の責務。今回§1–3と§4冒頭を確認。
- [G2] management `operations/ai_os/noah_operating_contract.md` — 今回currentの実行defaults・NOCOSIL継続・SCM境界を確認。旧Git Board優先の読替えは維持しない。

各参照は記した範囲の確認であり、個別文書全文を再監査したという意味ではない。文書が読めたこと、実装されたこと、利用者journeyが成立したことを分ける。

### 外部一次資料 — 確認範囲を個別に保持

[D1]–[D4]、[W1]–[W6]、[U1]は制定時2026-09-11の採用根拠を維持する。今回DADSトップのv2.18.0表示、[F1]、[C1][C2][C4]–[C10]、[W7][W8]の該当記述を2026-09-20に確認した。全リンク・全製品・全画面を同日に再検証したという意味ではない。料金や提供地域の現在値をこの一覧から推測しない。

- [D1] [デジタル庁：スタイルガイド](https://design.digital.go.jp/dads/guidance/style-guides/) — DADS v2.18.0のサイトを参照。ZUKAN独自のブランド・情報設計を重ねる根拠。
- [D2] [デジタル庁：アクセシビリティ](https://design.digital.go.jp/dads/guidance/accessibility/) — 完成サービス側での対応責務。
- [D3] [デジタル庁：カラー](https://design.digital.go.jp/dads/foundations/color/) — 色の役割・コントラスト・二重フォーカス。追加HEX値はDADS公式パレットの転記ではなくZUKANの設計値。
- [D4] [デジタル庁：タイポグラフィ](https://design.digital.go.jp/dads/foundations/typography/) — 16px基準、14px例外、書体と行高。
- [W1] [W3C：WCAG 2.2](https://www.w3.org/TR/WCAG22/) — Webアクセシビリティの規範。
- [W2] [W3C：Understanding Contrast Minimum](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html) — 文字コントラスト。
- [W3] [W3C：ARIA APG Read Me First](https://www.w3.org/WAI/ARIA/apg/practices/read-me-first/) — native意味論、ARIAと実動作の違い。
- [W4] [W3C：Understanding Target Size Minimum](https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html) — AAの24pxと例外。
- [W5] [W3C：Internationalization Quick Tips](https://www.w3.org/International/quicktips/)／[Personal Names](https://www.w3.org/International/questions/qa-personal-names) — 言語・方向・書式・氏名の多様さ。
- [W6] [W3C：Understanding Reflow](https://www.w3.org/WAI/WCAG22/Understanding/reflow.html) — reflowと二次元コンテンツの例外。
- [F1] [Google web.dev：Web Vitals](https://web.dev/articles/vitals) — LCP・INP・CLSの良好基準と75パーセンタイル。
- [U1] [USWDS：Design Principles](https://designsystem.digital.gov/design-principles/) — 実際の利用者の文脈、信頼、アクセシビリティ、継続的な評価。
- [C1] [iNaturalist：Explore screen](https://help.inaturalist.org/en/support/solutions/articles/151000198035-inaturalist-next-the-explore-screen) — iPhone向けExplore、map/list/grid、filter、Redo Search in Map Area。全OS/Identify画面の今回検証ではない。
- [C2] [Google Maps：Save favorite places](https://support.google.com/maps/answer/3184808?hl=en) — 保存と共有、オフラインdownloadの区別。タブ構成全体の再監査ではない。
- [C3] [Eventbrite：Using the Eventbrite app](https://www.eventbrite.com/help/en-us/articles/783059/) — v1.1の補助参照。今回の直接取得はHTTP 429で未確認。検索取得できた公式Tickets案内だけで全画面を検証済みとしない。当日のWeb復帰の採用根拠には[C8]を使う。
- [C4] [Wikimedia Codex：Design Principles](https://doc.wikimedia.org/codex/latest/style-guide/design-principles-overview.html) — Content First、Trustworthy、For Curious Humans。Codex current siteは国際化・accessibilityをdesign systemの機能として扱う。
- [C5] [AllTrails：Access downloaded maps](https://support.alltrails.com/hc/en-us/articles/37213988725780-How-to-access-your-downloaded-maps) — Saved/Downloadsと事前downloadの確認。契約planや機能をZUKANへ移植する根拠ではない。
- [C6] [Airbnb：Wishlists](https://www.airbnb.com/help/article/1236) — 保存した候補と日付条件の見直し。自動的な検索条件保存や共有をZUKANへ採用する根拠ではない。
- [C7] [Google Photos：Manage featured memories](https://support.google.com/photos/answer/9454489?co=GENIE.Platform%3DAndroid&hl=en) — 再表示・通知・非表示の本人制御。顔認識の導入要求ではない。
- [C8] [Peatix：Access tickets in a browser](https://help-attendee.peatix.com/en/support/solutions/articles/44001821804-how-can-i-access-my-tickets-in-the-pc-smartphone-browser-) — 購入時アカウント、Web表示・印刷・onlineイベントへの入口。
- [C9] [ArcGIS StoryMaps：Add sidecars](https://doc.esri.com/en/arcgis-storymaps/latest/author-and-share/add-sidecars.html) — 本文と媒体の関係、小画面の積み重ね。実装libraryの採用ではない。
- [C10] [GOV.UK：Check answers](https://design-system.service.gov.uk/patterns/check-answers/) — 重要な送信前の確認と変更後の復帰。
- [W7] [W3C：Dragging Movements](https://www.w3.org/WAI/WCAG22/Understanding/dragging-movements.html) — SC 2.5.7、dragなしの単一pointer代替。keyboard対応だけと同義ではない。
- [W8] [W3C：Focus Not Obscured Minimum](https://www.w3.org/WAI/WCAG22/Understanding/focus-not-obscured-minimum.html) — SC 2.4.11、固定要素等でfocus対象を完全に隠さない。

**世界標準を目指すとは、世界中で同じ見た目を押し付けることではない。誰が、どこで使っても、記録を失わず、意味を誤らず、自分の意思で地域に関われる共通品質を育てることである。** 本書はそのZUKAN内部の設計標準であり、国際標準団体の認定や全地域での検証済みという主張ではない。
