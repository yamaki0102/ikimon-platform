<?php

/**
 * VPS deployment template.
 * Copy to upload_package/config/secret.php on the target host and fill values.
 * Never commit the real secret.php.
 */

// Set to false to allow search engine indexing (production)
// define('NOINDEX_SITE_OVERRIDE', false);

if (!defined('GEMINI_API_KEY_SECRET')) {
    define('GEMINI_API_KEY_SECRET', '');
}

if (!defined('OPENAI_API_KEY_SECRET')) {
    define('OPENAI_API_KEY_SECRET', '');
}

if (!defined('GOOGLE_CLIENT_ID')) {
    define('GOOGLE_CLIENT_ID', '');
}

if (!defined('GOOGLE_CLIENT_SECRET')) {
    define('GOOGLE_CLIENT_SECRET', '');
}

if (!defined('TWITTER_CLIENT_ID')) {
    define('TWITTER_CLIENT_ID', '');
}

if (!defined('TWITTER_CLIENT_SECRET')) {
    define('TWITTER_CLIENT_SECRET', '');
}

// ── GA4 週次レポート設定（マルチサイト対応） ────────────────────────────────────
// Google Cloud Console でサービスアカウントを作成し、各GA4プロパティに「閲覧者」権限を付与すること
//
// サービスアカウントJSON はWebルート外に配置すること:
//   mkdir -p /var/www/ikimon.life/secrets
//   chmod 700 /var/www/ikimon.life/secrets
//
// 実行:
//   php upload_package/scripts/ga4_weekly_report.php          # 全サイト
//   php upload_package/scripts/ga4_weekly_report.php ikimon   # 特定サイトのみ
//
// Cron (毎週月曜 9:00 JST):
//   0 0 * * 1 php /var/www/ikimon.life/repo/upload_package/scripts/ga4_weekly_report.php >> /var/log/ikimon/ga4_report.log 2>&1

if (!defined('GA4_SITES')) {
    define('GA4_SITES', [
        [
            'slug'             => 'ikimon',
            'name'             => 'ikimon.life',
            'description'      => '市民参加型生物多様性プラットフォーム。ユーザーが自然観察を投稿し、生物同定・図鑑閲覧・BioBlitzで生物多様性データを蓄積する100年生態系アーカイブ。',
            'property_id'      => '386331654',
            'credentials_path' => '',   // 例: '/var/www/ikimon.life/secrets/ga4-service-account.json'
            'recipient_email'  => 'yamaki0102@gmail.com',
        ],
        [
            'slug'             => 'growup',
            'name'             => 'グロウアップアカデミー',
            'description'      => '学習者の成長を支援するオンラインアカデミー。',
            'property_id'      => '517210769',
            'credentials_path' => '',   // ikimon と同じサービスアカウントJSONを使用
            'recipient_email'  => 'yamaki0102@gmail.com',
        ],
        // ── サイトを追加する場合はここにブロックをコピー ────────────────────────
        // [
        //     'slug'             => 'mysite',
        //     'name'             => 'mysite.com',
        //     'description'      => 'サービスの説明（Geminiプロンプトに使われる）',
        //     'property_id'      => '',
        //     'credentials_path' => '/var/www/ikimon.life/secrets/ga4-service-account.json',
        //     'recipient_email'  => 'yamaki0102@gmail.com',
        // ],
    ]);
}
