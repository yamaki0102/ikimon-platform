<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name="life.ikimon.IkimonApp"
        android:label="いきものフィールド"
        android:icon="@android:drawable/ic_menu_compass"
        android:usesCleartextTraffic="${usesCleartextTraffic}"
        android:theme="@style/Theme.IkimonPocket"
        android:supportsRtl="true">

        <activity
            android:name="life.ikimon.ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.IkimonPocket">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="ikimonfieldscan"
                    android:host="auth"
                    android:pathPrefix="/callback" />
            </intent-filter>
        </activity>

        <!-- Pocket Mode Foreground Service -->
        <service
            android:name="life.ikimon.pocket.PocketService"
            android:foregroundServiceType="location|microphone"
            android:exported="false" />

        <!-- Field Companion Foreground Service (audio/location only; Nano 4 vision runs in foreground Activity) -->
        <service
            android:name="life.ikimon.pocket.FieldScanService"
            android:foregroundServiceType="location|microphone"
            android:exported="false" />

        <!-- Background Sync Worker -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            tools:node="remove" />

    </application>
</manifest>
