# Top / Home / My Page 外部レビュー採用ログ

## 実行状態

- Gemini: complete / `gemini-3.5-flash`
- Claude wrapper 1回目・再試行: metadataはokだが本文が対象読込途中で終了したため未完として扱った
- Claude直接読取専用再実行: complete / `claude-opus-4-8`
- Geminiの失敗候補: なし
- モデルdocs確認: 不要

## 採用

- 正本レンダラーを実際に呼び、そのHTMLへWorkerの本人データを差し込む統合テストを追加した。手書きテンプレートだけでは同じ契約ずれを再発検知できないという指摘を採用。
- 生成写真にロケール別の小さな「イメージ」ラベルを追加した。投稿由来の写真には付けず、生成写真との出所を視覚的に分けた。
- 公開Topの写真配信条件を再確認した。Topは公開専用read modelの行だけを読み、写真は `public_derivative_verified_at`、`exif_scrub_state='scrubbed'`、`public_ready_at` を満たす公開派生物だけを使用する。公開位置は正確座標を返さない既存gateを維持する。
- 端末画像は既存アップロード処理がSharpで再エンコードし、EXIF/IPTC/XMPを保持しないことを再確認した。追加モーダルではなく既存のサーバー側fail-closed処理を維持する。
- 「最後の1枚 → 今日撮る → 最近の記録 → 場所」と、My Pageを自己管理面にする役割分離を維持した。

## 不採用・調整

- Top専用の第二の掲載同意フラグ追加は不採用。現行の公開専用read modelとpublic-ready gateが公開面の正本であり、今回だけ別のDB同意概念を追加すると公開範囲の正本が二重化する。公開リスク対象・非公開記録・未scrub画像は既存gateで除外される。
- 端末画像選択の前後に追加の位置同意モーダルを強制する案は不採用。既存の画像再エンコードと公開位置gateで安全を担保しており、撮影入口の摩擦を戻すため。
- Homeの全カードへ公開状態バッジを追加する案は保留。Homeは再訪・次行動面、公開範囲の確認・変更は記録詳細とMy Pageへ集約する。内部状態や補助バッジをHomeの主役にしない方針を優先した。
- 貢献インパクト・ダッシュボード、A/Bテスト、ローカルSEO、写真一覧のMy Page復活は今回の本筋外として不採用。

## 出荷判断

正本統合テスト、生成画像の出所表示、EXIF/public-ready/位置保護の既存gate確認を完了した後、staging-firstで本番昇格可能と判断する。
